export interface Theme {
  name: string;
  colors: {
    background: string;
    foreground: string;
    primary: string;
    secondary: string;
    tertiary: string;
    accent: string;
    card: string;
    cardForeground: string;
    border: string;
    input: string;
  };
}
